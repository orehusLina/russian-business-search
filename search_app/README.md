# RB.RU Search - Веб-приложение для поиска

Красивое веб-приложение на Flutter для поиска по статьям RB.RU с использованием Elasticsearch.

## Структура проекта

```
search_app/
├── lib/                    # Flutter приложение
│   ├── main.dart          # Точка входа
│   ├── models/            # Модели данных
│   ├── services/          # Сервисы для API
│   ├── screens/           # Экраны приложения
│   └── widgets/           # Виджеты UI
├── backend/               # Python FastAPI бэкенд
│   ├── main.py           # API сервер
│   ├── index_data.py     # Скрипт индексации
│   └── requirements.txt  # Python зависимости
└── web/                  # Веб-конфигурация
```

## Установка и запуск

### 1. Установка зависимостей Flutter

```bash
cd search_app
flutter pub get
```

### 2. Установка и запуск Elasticsearch

```bash
# Через Docker (рекомендуется)
docker run -d -p 9200:9200 -p 9300:9300 \
  -e "discovery.type=single-node" \
  -e "xpack.security.enabled=false" \
  elasticsearch:8.11.0

# Или скачайте с https://www.elastic.co/downloads/elasticsearch
```

Проверьте подключение:
```bash
curl http://localhost:9200
```

### 3. Установка Python зависимостей

```bash
cd backend
pip install -r requirements.txt
```

### 4. Индексация данных

Перед запуском приложения нужно проиндексировать данные:

```bash
python backend/index_data.py
```

Это создаст индекс `rb_articles` в Elasticsearch и загрузит все статьи из `rb_articles.json`.

### 5. Запуск бэкенда

```bash
cd backend
python main.py
```

Или через uvicorn:
```bash
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

API будет доступен по адресу: http://localhost:8000
Документация API: http://localhost:8000/docs

### 6. Запуск Flutter приложения

```bash
cd search_app
flutter run -d chrome
```

Или для сборки веб-версии:
```bash
flutter build web
```

Затем откройте `build/web/index.html` в браузере.

## Возможности

- 🔍 **Полнотекстовый поиск** по статьям
- 🎨 **Красивый современный UI** с Material Design 3
- 📱 **Адаптивный дизайн** для разных размеров экрана
- 🔎 **Фильтры** по типу контента, компаниям, тегам
- 📊 **Статистика** по индексу
- ⚡ **Быстрый поиск** с использованием Elasticsearch
- 🌙 **Темная тема** (автоматическое определение)

## API Endpoints

- `GET /` - информация о API
- `GET /health` - проверка здоровья сервиса
- `POST /search` - поиск статей
- `GET /search?q=...` - поиск статей (GET версия)
- `GET /stats` - статистика по индексу

## Переменные окружения

Для бэкенда:
- `ELASTICSEARCH_HOST` - хост Elasticsearch (по умолчанию: localhost)
- `ELASTICSEARCH_PORT` - порт Elasticsearch (по умолчанию: 9200)

## Разработка

### Структура Flutter приложения

- **models/** - модели данных (Article, SearchResponse, Stats)
- **services/** - сервисы для работы с API (SearchService)
- **screens/** - основные экраны (SearchScreen)
- **widgets/** - переиспользуемые виджеты (ArticleCard, SearchBar, FiltersPanel)

### Структура бэкенда

- **main.py** - FastAPI приложение с эндпоинтами
- **index_data.py** - скрипт для индексации данных в Elasticsearch

## Лицензия

MIT
