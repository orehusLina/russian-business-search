# Скрапер для сайта rb.ru

Скрапер для сбора контента с сайта [rb.ru](https://rb.ru/) - российского издания о бизнесе, стартапах и технологиях.

## Особенности

Скрапер собирает следующие данные:
- **Базовые поля**: заголовок, текст, автор, дата публикации, URL
- **Метаданные**: теги, категории, описание
- **Уникальные поля для rb.ru**:
  - **Денежные суммы** - извлекает упоминания денег с полной информацией (сумма, валюта, единица измерения)
    - Пример: `{"amount": "220", "multiplier": "млн", "currency": "₽", "original": "220 млн ₽"}`
  - **Компании и стартапы** - находит упоминания компаний в кавычках и тексте
  - **Имена людей** - извлекает имена предпринимателей и фаундеров
  - **Тип контента** - определяет тип материала (новость, история, мнение, нейропрофайл и т.д.)

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

📖 **Быстрый старт:** См. `QUICKSTART.md` для пошаговых инструкций

## Использование

### 🧪 Тестирование на небольшом объеме данных

Для быстрой проверки работы скрапера на небольшом количестве данных:

```bash
python test_small.py
```

**Что делает:**
- Скрапит только раздел "новости" (`news`)
- Обрабатывает 2 страницы
- Использует 3 потока с задержкой 1.5 секунды
- Сохраняет результаты в `test_articles.json` и `test_articles.csv`

**Время выполнения:** ~2-5 минут  
**Ожидаемый результат:** ~20-50 статей

### 🚀 Полный скрапинг (большой объем)

Для сбора данных со всех разделов сайта:

```bash
python main.py
```

**Что делает:**
- Скрапит все разделы: новости, истории, мнения, нейропрофайлы, обзоры, чек-листы
- По умолчанию: 20 страниц из каждого раздела
- Использует 5 потоков с задержкой 1.0 секунда
- Сохраняет результаты в `rb_articles.json` и `rb_articles.csv`

**Время выполнения:** ~1-3 часа (зависит от скорости интернета)  
**Ожидаемый результат:** 5,000-20,000 статей

### ⚙️ Настройка параметров

Вы можете изменить параметры в файле `main.py`:

```python
# Для быстрого скрапинга (меньше данных)
scraper = RBScraper(
    max_workers=3,      # Меньше потоков
    delay=1.5          # Больше задержка
)
articles = scraper.scrape_all(max_pages_per_section=5)  # Меньше страниц

# Для максимального объема (больше данных)
scraper = RBScraper(
    max_workers=10,     # Больше потоков
    delay=0.5          # Меньше задержка (осторожно!)
)
articles = scraper.scrape_all(max_pages_per_section=50)  # Больше страниц
```

### 📊 Скрапинг конкретного раздела

Если нужно скрапить только один раздел:

```python
from scraper import RBScraper

scraper = RBScraper(max_workers=5, delay=1.0)

# Скрапим только новости
articles = scraper.scrape_section('news', max_pages=10)

# Сохраняем результаты
scraper.save_to_json('news.json')
scraper.save_to_csv('news.csv')
```

**Доступные разделы:**
- `news` - Новости
- `stories` - Истории
- `opinions` - Мнения
- `neuroprofiles` - Нейропрофайлы
- `reviews` - Обзоры
- `checklists` - Чек-листы

## Формат данных

Данные сохраняются в двух форматах (можно выбрать один, см. `FORMATS.md`):

### JSON (`rb_articles.json`)
```json
{
  "url": "https://rb.ru/news/...",
  "title": "Заголовок статьи",
  "content_type": "news",
  "author": "Имя автора",
  "date": "14 ноября 2025, 18:30",
  "tags": ["Тег1", "Тег2"],
  "categories": ["Категория1"],
  "text": "Полный текст статьи...",
  "description": "Краткое описание",
  "companies": ["Компания1", "Компания2"],
  "people": ["Иван Иванов", "Петр Петров"],
  "money": [
    {"amount": "220", "multiplier": "млн", "currency": "₽", "original": "220 млн ₽"},
    {"amount": "15", "multiplier": "млн", "currency": "$", "original": "$15 млн"}
  ],
  "scraped_at": "2025-11-14T20:30:00"
}
```

### CSV (`rb_articles.csv`)
Все поля сохраняются в CSV, списки объединяются через `; `.

## Обработка ошибок

Скрапер включает:
- Автоматические повторы при ошибках (3 попытки)
- Экспоненциальная задержка при ошибках
- Логирование всех операций в `scraper.log`
- Обработка таймаутов и сетевых ошибок
- Пропуск дубликатов URL

## Многопоточность

Скрапер использует многопоточность для ускорения сбора данных. По умолчанию используется 5 потоков, что позволяет собирать данные быстрее, сохраняя при этом разумную нагрузку на сервер.

## Логирование

Все операции логируются в файл `scraper.log` и выводятся в консоль. Уровень логирования: INFO.

## Примечания

- Скрапер уважает задержки между запросами для снижения нагрузки на сервер
- Используются случайные User-Agent для имитации обычных браузеров
- Все данные сохраняются с кодировкой UTF-8
- Скрапер автоматически определяет структуру страниц и адаптируется к изменениям

## Улучшенная версия

Также доступна улучшенная версия скрапера (`scraper_improved.py`) с дополнительными возможностями:

- ✅ **Нормализация дат** - даты парсятся в datetime объекты и ISO формат
- ✅ **Валидация данных** - проверка обязательных полей
- ✅ **Улучшенное извлечение инвестиций** - нормализация валют и сумм
- ✅ **Улучшенное извлечение компаний** - использование словаря известных компаний
- ✅ **Статистика** - автоматический подсчет статистики по данным
- ✅ **Отслеживание ошибок** - список неудачных URL для повторной попытки

Запуск улучшенной версии:
```bash
python scraper_improved.py
```

## Структура проекта

Проект организован в модульную структуру для лучшей поддерживаемости:

```
russian-business/
├── scraper/              # Пакет скрапера
│   ├── __init__.py       # Экспорт основного класса
│   ├── config.py         # Константы и настройки
│   ├── http_client.py    # HTTP запросы и загрузка страниц
│   ├── parsers.py        # Парсинг HTML страниц
│   ├── extractors.py     # Извлечение уникальных полей
│   ├── storage.py        # Сохранение данных (JSON, CSV)
│   └── scraper.py        # Основной класс скрапера
├── main.py               # Точка входа (использует модульную структуру)
├── test_small.py         # Тестовый скрипт для проверки на небольшом количестве данных
└── requirements.txt
```

### Преимущества модульной структуры:

- ✅ **Разделение ответственности** - каждый модуль отвечает за свою задачу
- ✅ **Тестируемость** - можно тестировать каждый модуль отдельно
- ✅ **Переиспользование** - можно использовать компоненты в других проектах
- ✅ **Читаемость** - меньше строк в каждом файле, легче найти нужный код
- ✅ **Масштабируемость** - легко добавлять новые модули

Подробнее о архитектуре см. `ARCHITECTURE.md`.

## Анализ реализации

Подробный анализ реализации, сравнение с другими вариантами и рекомендации по улучшению находятся в файле `ANALYSIS.md`.

## Примеры использования собранных данных

Собранные данные можно использовать для:
- Поиска статей по компаниям или людям
- Анализа упоминаний инвестиций
- Поиска материалов по тегам или категориям
- Анализа контента по типам материалов
- Построения дашбордов и аналитики

# russian-business-search
