# 🚀 Как запустить скрапер

## Быстрый старт

### 1️⃣ Небольшой объем (тестирование)

```bash
python test_small.py
```

- ⏱️ Время: 2-5 минут
- 📊 Результат: ~20-50 статей
- 💾 Файлы: `test_articles.json`, `test_articles.csv`

### 2️⃣ Большой объем (полный скрапинг)

```bash
python main.py
```

- ⏱️ Время: 1-3 часа
- 📊 Результат: 5,000-20,000 статей
- 💾 Файлы: `rb_articles.json`, `rb_articles.csv`

## Настройка объема

Отредактируйте `main.py`, изменив параметры:

```python
# Небольшой объем (~500-1,000 статей, ~15-30 минут)
scraper = RBScraper(max_workers=3, delay=1.5)
articles = scraper.scrape_all(max_pages_per_section=5)

# Средний объем (~5,000-10,000 статей, ~1-2 часа) - по умолчанию
scraper = RBScraper(max_workers=5, delay=1.0)
articles = scraper.scrape_all(max_pages_per_section=20)

# Большой объем (~15,000-30,000 статей, ~3-5 часов)
scraper = RBScraper(max_workers=10, delay=0.5)
articles = scraper.scrape_all(max_pages_per_section=50)
```

## Параметры

- `max_workers` - количество потоков (больше = быстрее, но риск блокировки)
- `delay` - задержка между запросами в секундах (меньше = быстрее, но риск блокировки)
- `max_pages_per_section` - количество страниц на раздел (больше = больше данных)

## Скрапинг одного раздела

```python
from scraper import RBScraper

scraper = RBScraper()
articles = scraper.scrape_section('news', max_pages=10)
scraper.save_to_json('news.json')
```

## Что дальше?

После скрапинга данные будут в JSON и CSV. Используйте их для:
- Анализа данных (pandas, numpy)
- Поиска по компаниям/людям/инвестициям
- Визуализации (matplotlib, plotly)
- Загрузки в БД (SQLite, PostgreSQL)

